function Snackbar(message) {
    return (
        <Snackbar
            // open={open}
            autoHideDuration={6000}
            // onClose={handleClose}
            message={message}
            // action={action}
        />
    );
}
